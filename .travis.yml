language : python
python:
    - "3.4"
before_install:
    - PATH=$PATH:/usr/local/lib
    - echo $PATH
    - chmod -R 0777 /usr/local/lib
    - sudo git clone https://github.com/lloyd/yajl.git
    - cd yajl
    - sudo ./configure
    - sudo make install
    - cd ..
install:
    - pip install again
    - pip install pytest
script: py.test
